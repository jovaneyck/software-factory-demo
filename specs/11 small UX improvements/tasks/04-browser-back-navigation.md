# Fix browser back button navigation

The browser back button is not working as expected. Implement proper back-navigation behavior for the SPA so that the browser's back button navigates through the app's route history correctly.
