# Unified training plan visualization component

The training plan is visualized differently in the plan view and the dog profile view. Create a single shared component based on the training plan page and reuse it in both pages.

# Rework training plan tests

After extracting the shared training plan component, rework all related tests to eliminate duplicate testing. The shared component should be tested in isolation.
