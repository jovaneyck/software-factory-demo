# 05 — Time range filter

Add a from/to date filter on the progress graph view.

- Defaults to showing "all" sessions for the selected dog + training
- Provide preset options: week, month, year, all
