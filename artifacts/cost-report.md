# Factory Cost and Execution Report

| Role | Execution | Token Usage | Monetary Cost |
| --- | --- | --- | --- |
| Foreman, feature owner, implementation | This Copilot session | Not exposed to this session | Not available |
| Independent adversarial reviewer | One Copilot subagent invocation | Not returned by the tool | Not available |
| Herdr / sandboxed pi workers | Not used, per user request | Not applicable | Not applicable |

The normal `factory-cost-report.sh` reads Herdr/pi session telemetry. Running it would require the explicitly excluded execution environment. No token or dollar totals have been invented, and unavailable telemetry must not be interpreted as zero cost.

Requested PR attribution: **Built by gpt 6 astra.** This is the user's requested attribution; this session does not expose provider billing metadata to independently verify a model-specific cost.

The run used one implementation session, one independent review round, and one fix pass. No auto-merge, continuous watcher, sandbox, or Herdr dashboard was started.